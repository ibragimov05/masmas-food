import 'package:flutter/material.dart';

class PageMaker extends StatelessWidget {
  const PageMaker({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
